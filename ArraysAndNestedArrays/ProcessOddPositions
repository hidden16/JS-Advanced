function solve(array){
    let result = [];
    for(let i = 0; i < array.length; i++){
        if(i % 2 == 1){
            array[i] *= 2;
            result.push(array[i]);
        }
    }
    return result.reverse();
}
solve([10, 15, 20, 25])